program Team_pp;

class Persona {
    attributes
        int : edad, identificador;
        float : matrix[10, 5];

    methods
        function int UNO (int : x) {
            return (edad - x);
        };
};

hivar
    int : i, j, p;
    Persona : estudiante;
byevar

function int fact (int : j) {
    hivar
        int : i;
    byevar

    i = j + (p - j * 2 + j);
    if (j == 1) {
        return (j);
    } else {
        return (j * fact(j - 1));
    };
};

function void pelos (int : y) {
    hivar
        int : x, res;
    byevar

    read(estudiante.edad);
    x = y;
    while (x < 10) do {
        res = !estudiante.UNO(x);
        write(res);
        x = x + 1;
    };
};

main() {
    read(p);
    j = p * 2;
    i = fact(p);
    write("dame el id del estudiante: ");
    read(i);
    estudiante.identificador = i;
    pelos(p);
    from i = 1 to 10 do {
        write("HelloWorld \n");
        write(fact(estudiante.edad));
    };
};