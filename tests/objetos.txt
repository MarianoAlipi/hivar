program objetos_pp;

class Fecha {
    attributes
        int : anio, dia, mes;

    methods
        function int multiplica (int : x) {
            return (dia * x);
        };

        function void print_fecha() {
            write(dia);
            write("\n");
            write(mes);
            write("\n");
            write(anio);
            write("\n");
        };
};

hivar
    int : res;
    Fecha : hoy;
byevar

main() {
    hoy.dia = 2;
    hoy.mes = 5;
    hoy.anio = 2021;
    !hoy.print_fecha();
    res = !hoy.multiplica(10);
    write(res);
    write("\n");
};
