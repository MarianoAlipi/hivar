program recursion;

hivar
    int : i, j, p;
byevar

function int fact (int : j) {
    hivar
        int : i;
    byevar
    
    if (j == 0) {
        return (1);
    } else {
        i = fact(j - 2);
        return (j * i);
    };
    
};


main() {
    j = 10;
    i = fact(j);
    write(i);
};